language: java

install: true

jdk:
  - oraclejdk8

before_install:
 - chmod +x gradlew


script:
  - ./gradlew build

# "group": "stable",
#   "dist": "precise",
#   "os": "linux"

# before_cache:
# - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
# - rm -fr $HOME/.gradle/caches/*/plugin-resolution/
#
# cache:
#   directories:
#     - $HOME/.gradle/caches/
#     - $HOME/.gradle/wrapper/
